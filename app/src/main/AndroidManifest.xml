<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="miu.com.pos_miu_application">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.Activities.LoginActivity"></activity>
        <activity android:name=".views.Activities.SplashScreenActivity" />
        <activity android:name=".views.Order.CreateOrderActivity" />
        <activity android:name=".views.SplashScreen.SplashScreenActivity" />
        <activity android:name=".views.Settings.SettingActivity" />
        <activity android:name=".views.Products.ProductsActivity" />
        <activity android:name=".views.Category.CategoryActivity" />
        <activity android:name=".views.Order.OrderActivity" />
        <activity android:name=".views.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>