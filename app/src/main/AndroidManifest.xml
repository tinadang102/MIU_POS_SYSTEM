<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="miu.com.pos_miu_application">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.Activities.item_product"></activity>
        <activity android:name=".views.Activities.item_orderdetail" />
        <activity android:name=".views.Activities.delivery" />
        <activity android:name=".views.Activities.custom_product" />
        <activity android:name=".views.Activities.addCustomer" />
        <activity android:name=".views.Activities.LoginActivity" />
        <activity android:name=".views.Activities.MainActivity">
            <intent-filter>
                <action
                    android:name="android.intent.action.MAIN"
                    android:screenOrientation="landscape" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>